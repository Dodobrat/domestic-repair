<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <meta name="description" content="Something went wrong">
    <meta name="author" content="Deyan Bozhilov">
    <title>Report from {{user.user}}</title>
    <script src="https://kit.fontawesome.com/0782d985f5.js" crossorigin="anonymous"></script>
    <link href="../style.css" type="text/css" rel="stylesheet"/>
</head>
<body>
<nav class="navbar">
    <div class="container nav-container">
        <a class="nav-title" href="/">Domestic Repair</a>
        <ul>
            <li>
                <img src="../{{user.avatar}}" alt="">
                Hi, {{user.user}}
            </li>
            <a href="/logout">
                <li>
                    <i class="fas fa-sign-out-alt"></i> Logout
                </li>
            </a>
        </ul>
    </div>
</nav>
<div class="container">
    <a href="/" class="go-back">
        <button><i class="fas fa-arrow-left"></i> Go Back</button>
    </a>
    Status:
    {{#if job.assigned}}
        {{#if job.status}}
            <i class="fas fa-check text-green"></i>
        {{else}}
            <i class="fas fa-times text-red"></i>
        {{/if}}
    {{else}}
        {{# if job.quoteId}}
            <i class="fas fa-spinner"></i>
        {{else}}
            <i class="fas fa-times text-red"></i>
        {{/if}}
    {{/if}}
    <div class="job-details">
        <div class="keywords">
            <span> Appliance: <span class="tag">{{job.appType}}</span></span>
            <span> Age: <span class="tag">{{job.appAge}} years</span></span>
            <span> Manufacturer: <span class="tag">{{job.appMan}}</span></span>
        </div>
        <p class="created">by <span class="secondary">{{user.user}}</span> | created: <span class="secondary">{{job.createdAt}}</span></p>
        <div class="job-desc">
            <p>{{job.desc}}</p>
        </div>
        <span id="jobLat" style="opacity: 0; pointer-events: none">{{job.lat}}</span><span id="jobLng" style="opacity: 0; pointer-events: none">{{job.lng}}</span>
        <div id="jobMap"></div>
    </div>
    <br>
    {{#if job.quoteId}}
        {{#if quote}}
            <div class="job-details">
                <h3 class="text-center">Quote</h3>
                <div class="keywords">
                    <span> Price: <span class="tag">£ {{quote.price}}</span></span>
                    <span> Booked Date: <span class="tag">{{quote.executionDate}}</span></span>
                    <span> Booked Time-Slot: <span class="tag">{{quote.executionTime}}</span></span>
                </div>
                <p class="created">
                    A technician will come to the premises to execute the repair. His name is <span
                        class="secondary">{{tech.user}}</span>.
                    <br>
                    Please do not let them in before they have identified themselves. <br>
                    <strong>We do not take responsibility if you get killed by someone other than our employee</strong>

                    <br><br>
                    {{#if job.assigned}}
                        <p class="text-green">Thank you accepting our offer. The technician will execute the previously stated repair</p>
                        {{else}}
                        <a href="/assign/{{quote.jobId}}/{{quote.id}}">Approve Offer</a>
                        <a href="/terminate/{{quote.id}}">Decline Offer</a>
                    {{/if}}
                </p>
            </div>
        {{/if}}
    {{/if}}
    <br>
</div>

<script>
    function initMap() {
        let latInput = document.getElementById("jobLat").innerText
        let lngInput = document.getElementById("jobLng").innerText
        const mapOptions = {
            zoom: 14,
            center: new google.maps.LatLng(latInput,lngInput)
        }
        const map = new google.maps.Map(document.getElementById('jobMap'), mapOptions);
        let marker = new google.maps.Marker({
            position: mapOptions.center,
            map: map,
            draggable:false
        });
        map.setCenter(marker.position);
    }
</script>
<script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyClh5B6yiEc3qd754g2pItmh_FcgXAUr3E&callback=initMap"
        async defer></script>
</body>
</html>
